---
title: "DMuon: Efficient Distributed Muon Training with Near-Adam Overhead"
authors:
  - Vincent Chen
  - Starrick Liu
  - Regis Cheng
  - Dance Yang
  - Shalfun Li
  - Ryan Yu
  - Lucy Liang
  - Hang Su
  - Roy Gan
  - Hao Wang
  - Qian Wang
arxiv_id: "2606.27153"
aliases: []
date: "2026-06-25"
tags:
  - distributed-training
  - optimizer
  - muon
  - llm-training
  - systems-efficiency
url: "https://arxiv.org/abs/2606.27153"
---

> ⚠️ **Abstract-only summary** — The PDF for this paper could not be downloaded (404). The following analysis is derived solely from the arXiv abstract; claims about method internals, experimental setup, and detailed results are not independently verifiable from full text.

> **一句话总结：** DMuon 是一个开源的分布式 Muon 优化器实现，作为即插即用模块嵌入现有训练流水线，通过将代价高昂的 Newton-Schulz 矩阵正交化迭代优化并适配分布式系统，把每步延迟压缩到接近 AdamW 的水平，在具身基础模型和 LLM 训练负载上分别取得 1.48x–3.01x 的端到端提速与最高 163x 的优化器步提速。

> **TL;DR:** DMuon is an open-source, drop-in distributed Muon optimizer that fixes the >2x overhead of vanilla Muon (caused by matrix-coupled updates and costly Newton-Schulz iterations), achieving 1.48x–3.01x end-to-end step-time speedup and 6.85x–163x optimizer-step speedup over baseline Muon across embodied foundation-model and LLM workloads, bringing per-step latency close to AdamW.

## 核心贡献

- **首个高效分布式 Muon 实现**：针对矩阵级优化器（Muon）与现有元素级假设的分布式训练基础设施不匹配的问题，提出了 DMuon，将 Muon 的 Newton-Schulz 正交化开销大幅压缩。
- **即插即用、零框架改动**：以 drop-in 模块形式集成到现有训练流水线，无需框架层面的修改，工程落地门槛极低。
- **显著性能提升**：在具身基础模型与 LLM 两类负载上，端到端步时间提速 1.48x–3.01x，优化器步时间提速 6.85x–163x，使 Muon 的每步延迟逼近 AdamW。
- **开源贡献**：代码开源，便于社区复现与扩展矩阵级优化器在分布式场景下的应用。

## Key Contributions

- Identifies and quantifies the core inefficiency of Muon: its matrix-coupled updates and Newton-Schulz iterations make vanilla implementations cost more than 2x the forward+backward pass, a mismatch with element-wise-optimizer-centric distributed infrastructure.
- Delivers DMuon, an open-source distributed Muon implementation that integrates into existing training pipelines as a **drop-in module with no framework-level modifications**.
- Demonstrates large speedups across two distinct workload families — embodied foundation models and large language models — with **1.48x–3.01x end-to-end step-time** speedup and **6.85x–163x optimizer-step-time** speedup.
- Closes the per-step latency gap between Muon and AdamW, making matrix-orthogonalization optimizers practical for large-scale training.

## Methodology

Per the abstract, DMuon is a distributed implementation of Muon that targets the bottleneck of the Newton-Schulz iterations used to orthogonalize matrix updates. The system is designed to be a drop-in module that slots into existing distributed training pipelines without framework-level changes. It is evaluated on both embodied foundation-model training and LLM training workloads, measuring end-to-end step time and isolated optimizer-step time against baseline (vanilla) Muon, with per-step latency benchmarked against AdamW.

(Note: the abstract does not describe the specific algorithmic or systems techniques — e.g., overlap/communication-computation scheduling, tiled Newton-Schulz, gradient bucketing, or sequence-parallel reductions — used to remove the overhead. These details are not available without the full paper.)

## Results & Highlights

- End-to-end step-time speedup: **1.48x–3.01x** over baseline Muon.
- Optimizer-step-time speedup: **6.85x–163x** over baseline Muon.
- Per-step latency reduced to **near-AdamW levels**, effectively eliminating the practical cost disadvantage that previously held back Muon adoption.
- Validated across two workload types: embodied foundation models and large language models, suggesting workload-agnostic gains.
- Vanilla Muon baseline is reported to incur more than **2x the cost of forward+backward passes**, framing the magnitude of the solved problem.

## Authors & Credibility

- Eleven authors (Vincent Chen, Starrick Liu, Regis Cheng, Dance Yang, Shalfun Li, Ryan Yu, Lucy Liang, Hang Su, Roy Gan, Hao Wang, Qian Wang).
- Affiliations are not stated in the abstract; without the PDF, institutional context (e.g., whether this is an industry or university lab with real large-scale training infrastructure) cannot be confirmed.
- The work is positioned around concrete systems metrics (step time, optimizer-step time) and claims open-source availability, which is a positive signal for reproducibility.
- Credibility assessment is necessarily limited by abstract-only access; the headline numbers are internally consistent (large optimizer-step speedups naturally drive end-to-end gains) but not independently verifiable here.

## Critical Assessment

- **Novelty:** Moderate-to-high. Distributed Muon is a timely problem, and the framing of "element-wise-optimizer-centric infrastructure vs. matrix-level optimizers" is a genuine systems gap. Novelty hinges on whether DMuon introduces new scheduling/tiling techniques versus engineering integration — the abstract does not specify.
- **Rigor:** Cannot be fully assessed from the abstract. The reported ranges (especially the 6.85x–163x optimizer-step spread) suggest sensitivity to workload/model size; a rigorous paper would need to explain what drives this wide variance and provide matched-hardware baselines.
- **Reproducibility:** Positive signals — open-source, drop-in, no framework modifications. However, reproducibility cannot be confirmed without access to the actual code link and hardware/training-config details in the full text.
- **Significance:** High if the claimed near-AdamW latency holds across the LLM scale-up curve; this would materially lower the barrier to adopting Muon-class optimizers in production training.
- **Red flags:**
  - The **6.85x–163x optimizer-step** range is exceptionally wide — readers should scrutinize what configuration produces each end of the range and whether the 163x figure is representative or a best-case cherry pick.
  - "Near-AdamW" is not quantified in the abstract (no exact latency ratio given).
  - Baseline "vanilla Muon" is not precisely defined (implementation, Newton-Schulz iteration count, precision).
  - **Abstract-only access** prevents verification of all of the above.

## Strengths

- Directly addresses a real, well-motivated systems bottleneck (Newton-Schulz overhead) that has hindered Muon adoption.
- Practical engineering framing: drop-in module, no framework changes, open-source — lowers adoption friction.
- Reports both micro (optimizer-step) and macro (end-to-end step-time) metrics, allowing overhead attribution.
- Evaluated across two distinct workload families rather than a single model, strengthening generalization claims.

## Limitations

- **Abstract-only summary**: The PDF was unavailable (404), so all technical claims — internal mechanisms, baseline definitions, exact hardware, training configs, and statistical significance — are unverified.
- The headline speedup ranges are broad; without the paper it is unclear what drives the variance or whether representative configurations are reported.
- "Near-AdamW" latency is not quantified with a precise ratio.
- Affiliations, code-release URL, and peer-review status are unknown from the abstract alone.

## Relevance

与您关注的分布式训练与训练效率方向高度相关：DMuon 正是在分布式系统层面优化矩阵级优化器（Muon）的通信与计算开销，把昂贵的 Newton-Schulz 迭代开销压缩到接近 AdamW，是大模型高效训练与 scaling 场景下值得跟进的 systems 工作。

## Questions for Further Exploration

- What specific techniques (communication-computation overlap, tiled/streamed Newton-Schulz, reduced-precision iterations, gradient bucketing) produce the overhead reduction, and which contribute most?
- What drives the large spread in optimizer-step speedup (6.85x vs 163x) — model size, matrix dimensions, tensor-parallel degree, or Newton-Schulz iteration count?
- What is the exact Muon-to-AdamW per-step latency ratio after DMuon, and how does it scale with model size?
- How does DMuon compose with tensor/pipeline/data parallelism and ZeRO-style sharding — are there constraints on parallelism strategy?
- Does DMuon preserve Muon's reported convergence benefits at scale, or are any accuracy trade-offs introduced by the distributed approximation?
- Is the baseline "vanilla Muon" a publicly available implementation, ensuring a fair apples-to-apples comparison?
